# Uncomment and configure for remote state storage.
# Create the bucket and DynamoDB lock table first.
#
# terraform {
#   backend "s3" {
#     bucket         = "eve-horizon-tfstate"
#     key            = "aws/terraform.tfstate"
#     region         = "us-west-2"
#     dynamodb_table = "eve-horizon-tf-locks"
#     encrypt        = true
#   }
# }
