apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eve-dashboard
  namespace: eve
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - $patch: replace
      host: dashboard.eve.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eve-dashboard
                port:
                  number: 3200
