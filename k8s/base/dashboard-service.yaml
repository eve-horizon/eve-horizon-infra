apiVersion: v1
kind: Service
metadata:
  name: eve-dashboard
  namespace: eve
  labels:
    app.kubernetes.io/name: eve-dashboard
spec:
  selector:
    app.kubernetes.io/name: eve-dashboard
  ports:
    - name: http
      port: 3200
      targetPort: http
